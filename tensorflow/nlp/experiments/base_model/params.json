{
    "model_version": "lstm",
    "lstm_num_units": 128,
    "embedding_size": 100,

    "learning_rate": 1e-3,
    "batch_size": 32,
    "num_epochs": 10,
    "dropout_rate": 0.3,

    "save_summary_steps": 100
}
